{
    // Builds the dev container from .devcontainer/Dockerfile so this repo works
    // as a standalone template (and in forks) without needing pull access to a
    // private GHCR image. Configure Codespaces Prebuilds on your repo to make
    // first-launch fast. If you publish your own pre-built image, you can swap
    // this "build" block for: "image": "ghcr.io/<your-org>/<image>:latest".
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "remoteEnv": {
        "OLLAMA_MODEL": "llama3.2:3b"
    },
    "hostRequirements": {
        "cpus": 4,
        "memory": "16gb",
        "storage": "32gb"
    },
    "customizations": {
        "vscode": {
            "settings": {
                "python.terminal.activateEnvInCurrentTerminal": true,
                "python.defaultInterpreterPath": "${containerWorkspaceFolder}/py_env/bin/python",
                "github.copilot.enable": {
                    "*": false,
                    "plaintext": false,
                    "markdown": false,
                    "scminput": false
                },
                "github.copilot.enableAutoComplete": false,
                "editor.inlineSuggest.enabled": false,
                "github.copilot.inlineSuggest.enable": false,
                "github.copilot.chat.enable": false,
                "github.copilotChat.showInExplorer": false,
                "workbench.startupEditor": "readme",
                "workbench.editorAssociations": {
                    "*.md": "vscode.markdown.preview.editor"
                },
                "terminal.integrated.defaultProfile.linux": "bash",
                "terminal.integrated.profiles.linux": {
                    "bash": {
                        "path": "bash",
                        "args": [
                            "-l"
                        ]
                    }
                }
            },
            "extensions": [
                "mathematic.vscode-pdf"
            ]
        }
    },
    "postCreateCommand": "bash -i scripts/pysetup.sh py_env && bash -i scripts/startOllama.sh && py_env/bin/python scripts/warmup.py",
    "postAttachCommand": "bash scripts/post_attach_ollama.sh"
}
